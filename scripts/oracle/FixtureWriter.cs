// Generates test fixtures this repository cannot otherwise obtain.
// Filled in below; see `write` in Program.cs.
static class FixtureWriter {
	public static int Run(string[] args) {
		Console.Error.WriteLine("write: not implemented yet");
		return 2;
	}
}
